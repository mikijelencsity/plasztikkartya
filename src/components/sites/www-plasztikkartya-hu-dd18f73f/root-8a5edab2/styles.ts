// Shared class strings for recurring Elementor patterns on the source page.

/** `.responsive` custom class: 50px side padding, 25px at <=1340px. */
export const responsivePadding = "px-[25px] wide:px-[50px]";

/** Boxed Elementor container with --content-width: 1600px. */
export const boxedInner = "mx-auto w-full max-w-[1600px]";

/** Section H2 (65/75 desktop, 35 tablet, 25/40 mobile). */
export const sectionHeading =
  "w-fit max-w-full font-raleway text-[25px] leading-[40px] font-medium text-white tab:text-[35px] tab:leading-[75px] desk:text-[65px]";

/** Muted body copy (#FFFFFF66, 18/35 desktop, 16/35 tablet, 16/30 mobile). */
export const mutedText =
  "font-raleway text-[16px] leading-[30px] font-normal text-white/40 tab:leading-[35px] desk:text-[18px]";
